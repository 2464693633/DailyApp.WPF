<Window x:Class="DailyApp.WPF.Views.MainWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DailyApp.WPF.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}" 
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        Title="MainWin" Height="550" Width="1000">
    <!--无标题栏-->
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="-1"></WindowChrome>
    </WindowChrome.WindowChrome>
    <materialDesign:DialogHost DialogTheme="Inherit"
                            Identifier="RootDialog"
                            SnackbarMessageQueue="{Binding ElementName=MainSnackbar, Path=MessageQueue}">

        <materialDesign:DrawerHost IsLeftDrawerOpen="{Binding ElementName=MenuToggleButton, Path=IsChecked}" x:Name="drawerHost">
            <!--菜单-->
            <materialDesign:DrawerHost.LeftDrawerContent>
               
                <DockPanel MinWidth="220">
                    <!--图片-->
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                        <Image Source="/Images/超人1.png" Height="60" Width="60" Margin="0,18,8,0">
                            <!--裁剪成圆形-->
                            <Image.Clip>
                                <EllipseGeometry Center="30,30" RadiusX="40" RadiusY="40"/>
                            </Image.Clip>
                        </Image>
                    </StackPanel>
                    <!--菜单列表-->
                    <ListBox ItemsSource="{Binding LeftMenuList}" ItemContainerStyle="{StaticResource LeftMenuStyle}" x:Name="lbMenu" SelectionChanged="lbMenu_SelectionChanged">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <i:InvokeCommandAction Command="{Binding NavigateCmm}" CommandParameter="{Binding ElementName=lbMenu,Path=SelectedItem}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Background="Transparent">
                                    <materialDesign:PackIcon Margin="15,0,15,0" Kind="{Binding Icon}"></materialDesign:PackIcon>
                                    <TextBlock Text="{Binding MenuName}"></TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
               
                </DockPanel>
            </materialDesign:DrawerHost.LeftDrawerContent>
            <!--头部内容-->
            <DockPanel>
                <materialDesign:ColorZone Padding="16" materialDesign:ElevationAssist.Elevation="Dp4"
                                 DockPanel.Dock="Top" Mode="PrimaryMid"
                                 MouseDoubleClick="ColorZone_MouseDoubleClick">
                    
                    <DockPanel LastChildFill="True">
                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                            <Image Source="/Images/超人1.png" Height="32" Width="32" Margin="0,0,5,0">
                                <!--裁剪成圆形-->
                                <Image.Clip>
                                    <EllipseGeometry Center="12,12" RadiusX="16" RadiusY="16"/>
                                </Image.Clip>
                            </Image>
                            <Button x:Name="btnMin" Content="一" Style="{StaticResource MaterialDesignFlatMidBgButton}" Click="btnMin_Click"/>
                            <Button x:Name="btnMax" Content="口" Style="{StaticResource MaterialDesignFlatMidBgButton}" Click="btnMax_Click"/>
                            <Button x:Name="btnClose" Content="X" Style="{StaticResource MaterialDesignFlatMidBgButton}" Click="btnClose_Click"/>

                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal">
                            <ToggleButton x:Name="MenuToggleButton" AutomationProperties.Name="HamburgerToggleButton"                       
                           IsChecked="False" Style="{StaticResource MaterialDesignHamburgerToggleButton}" />

                            <Button Margin="24,0,0,0"     
                     Command="{Binding GoBackCmm}"
                     Content="{materialDesign:PackIcon Kind=ArrowLeft,
                                                       Size=24}"
                     Foreground="{Binding RelativeSource={RelativeSource AncestorType={x:Type FrameworkElement}}, Path=(TextElement.Foreground)}"
                     Style="{StaticResource MaterialDesignToolButton}"
                     ToolTip="Previous Item" />

                            <Button Margin="16,0,0,0"
                     
                     Command="{Binding GoForWardCmm}"
                     Content="{materialDesign:PackIcon Kind=ArrowRight,
                                                       Size=24}"
                     Foreground="{Binding RelativeSource={RelativeSource AncestorType={x:Type FrameworkElement}}, Path=(TextElement.Foreground)}"
                     Style="{StaticResource MaterialDesignToolButton}"
                     ToolTip="Next Item" />

                            <Button Margin="16,0,0,0"
                     
                     Command="{Binding HomeCommand}"
                     Content="{materialDesign:PackIcon Kind=Home,
                                                       Size=24}"
                     Foreground="{Binding RelativeSource={RelativeSource AncestorType={x:Type FrameworkElement}}, Path=(TextElement.Foreground)}"
                     Style="{StaticResource MaterialDesignToolButton}"
                     ToolTip="Home" />
                        </StackPanel>

                        <TextBlock Text="我的日常" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="16,0,0,0"/>


                    </DockPanel>
                </materialDesign:ColorZone>

                <!---->
                <ContentControl prism:RegionManager.RegionName="MainViewRegion"></ContentControl>
  
            </DockPanel>
        </materialDesign:DrawerHost>
    </materialDesign:DialogHost>
</Window>
